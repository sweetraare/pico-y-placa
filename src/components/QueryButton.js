import React, { useState } from "react";
import "../App.css";

function QueryButton() {
  return (
    <div className="d-flex j-end">
      <button className="btn query-button">Consultar</button>
    </div>
  );
}

export default QueryButton;
